# Copy to .env and set to your deployed backend URL for production builds.
# In development, Vite proxies /api to localhost:3001 — this variable isn't needed locally.
VITE_API_URL=https://rxregistry-backend.onrender.com/api/v1
